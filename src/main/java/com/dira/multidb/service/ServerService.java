package com.dira.multidb.service;

import com.dira.multidb.database1.Client;
import com.dira.multidb.database2.Server;

/**
 * Server service for client entity operations
 * 
 * @author PlayJava
 *
 */
public interface ServerService {
	public Server setServer(Server server);
}
